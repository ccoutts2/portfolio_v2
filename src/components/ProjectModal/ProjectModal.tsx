"use client";
import { AccordionItems } from "@/app/work/types";

interface ProjectModalProps {
  project: AccordionItems[];
}

const ProjectModal = ({ project }: ProjectModalProps) => {
  return <></>;
};

export default ProjectModal;
